const initalState = {
    products: [],
    error: false,
    loading: false
}

export default function (state = initalState, action) {
    switch (action.type) {
        case 'aa':
            
            break;
    
        default:
            return state
    }
}